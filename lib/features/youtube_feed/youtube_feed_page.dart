import 'package:flutter/material.dart';

class YoutubeFeedPage extends StatelessWidget {
  const YoutubeFeedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('YouTube')),
      body: const Center(
        child: Text('Stub'),
      ),
    );
  }
}
